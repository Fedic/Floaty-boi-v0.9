//Unused
/*
export default class MenuScene extends Phaser.Scene {
    constructor() {
      super({ key: 'menuScene' });
    }
  
    preload() {
      this.load.image('startButton', 'assets/start.png'); // Replace with your start button image path
    }
  
    create() {
      // Add start button image
      const startButton = this.add.image(config.width / 2, config.height / 2, 'startButton').setInteractive();
  
      // Add click event for start button
      startButton.on('pointerdown', () => {
        this.scene.start('mainScene');
      });
    }
  }
  */